<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration

    - Production: HTTPS only (no cleartext)
    - Debug: allows HTTP to localhost for development

    Reference: https://developer.android.com/training/articles/security-config
-->
<network-security-config>
    <!-- Production: force HTTPS everywhere -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust only system CA certificates (not user-installed CAs) -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Development override: allow HTTP to local server -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
